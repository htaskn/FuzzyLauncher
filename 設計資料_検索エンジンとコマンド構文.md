# FuzzyLauncher 設計資料 ― 検索エンジン／コマンド定義ファイル／変数解釈／独自構文

対象バージョン: PowerShell 5.1 + WinForms 実装（`FuzzyLauncher.ps1` 起点）

---

## 0. 全体構成

```mermaid
flowchart TB
    subgraph Startup["起動処理"]
        A1["FuzzyLauncher.ps1"] --> A2["Core/Interop.ps1<br>(KeyboardHook / WindowFinder)"]
        A1 --> A3["Utils/EnvironmentSync.ps1<br>レジストリ→環境変数 同期"]
        A1 --> A4["Core/AppSettings.ps1<br>settings.ini 読込"]
        A1 --> A5["Core/CommandListParser.ps1<br>commands/*.txt 読込"]
        A1 --> A6["Core/BuiltinCommandManager.ps1<br>&amp;コマンド自動登録"]
        A1 --> A7["UI/FuzzySearcher.psm1<br>あいまい検索ポップアップ"]
        A1 --> A8["UI/LauncherForm.ps1<br>アプリ固有ラッパー"]
    end

    A5 --> M["$script:Menus<br>Dictionary&lt;menu, CommandItem[]&gt;"]
    A8 --> M
    A7 -.検索対象として参照.-> M
```

- **検索エンジン**（`UI/FuzzySearcher.psm1`）は完全にアプリ非依存の汎用コンポーネント。`(Name, Caption)` の一覧と入力文字列を受け取り、スコア順に並べ替えるだけ。
- **コマンド定義**（`Core/CommandListParser.ps1`）は `commands/*.txt` を読み、`$script:Menus` に格納する。
- **変数解釈**は「読み込み時（`${...}`）」と「実行時（`%ENV%`）」の2段階に分かれている。
- **独自構文**（`&builtin`, `;`連結, `\`エスケープ）は `Core/BuiltinCommandManager.ps1` と `UI/LauncherForm.ps1` の実行フェーズで解釈される。

---

## 1. 検索エンジン（あいまい検索）

実装: `UI/FuzzySearcher.psm1`

### 1.1 スコアリングアルゴリズム

`Get-FuzzyMatchScore(target, query)` は、クエリ文字を先頭から貪欲に target 内へ割り当てていく **左→右・貪欲・部分列マッチ**（Sublime/VSCode 系のあいまい検索と同系統）。

```mermaid
flowchart TD
    Start(["クエリ文字 c を1文字ずつ処理"]) --> Consecutive{"直前マッチ位置+1が\nc と一致?"}
    Consecutive -- Yes --> Hit1["その位置で確定\n+MatchScore +ConsecutiveMatchBonus"]
    Consecutive -- No --> Scan["target を左から走査し\n未消費の c を探す"]
    Scan --> Found{"見つかった?"}
    Found -- Yes --> Abbrev{"先頭 / 大文字 / \n区切り文字直後?"}
    Abbrev -- Yes --> Hit2["+MatchScore +AbbreviationMatchBonus"]
    Abbrev -- No --> Hit3["+MatchScoreのみ"]
    Found -- No --> Miss["-MismatchPenalty\n(直前も不一致なら\n-ConsecutiveMismatchPenaltyも)"]
    Hit1 --> Consume["一致位置を'*'化して消費済みにする"]
    Hit2 --> Consume
    Hit3 --> Consume
    Consume --> Next["lastMatchIndex 更新 → 次のクエリ文字へ"]
    Miss --> Reset["lastMatchIndex = -1（連続ボーナス途切れ）→ 次のクエリ文字へ"]
    Next --> Start
    Reset --> Start
```

- 1つの target 文字は最大1回しか消費されない（マッチ済みは `*` に置換）。
- スコア定数は `settings.ini` の `[Scoring]` セクションで変更可能（既定値はいずれも 20 / 10 系）。
- 戻り値は `{ Score, Bingo }`。`Bingo` はマッチ箇所を `*` に置き換えた文字列（デバッグ表示用）。

| target | query | スコア | 内訳 |
|---|---|---|---|
| `notepad` | `no` | 80 | n: 先頭+略語(20+20), o: 連続(20+20) |
| `notepad` | `z` | -10 | 不一致1回 |
| `notepad` | `zz` | -30 | 不一致 -10、連続不一致 -10-10 |
| `my_command` | `mc` | 80 | `_` の直後の `c` が略語ボーナス |
| `MyCommand` | `mc` | 80 | 大文字 `C` が略語ボーナス |

### 1.2 インクリメンタル検索のシーケンス

```mermaid
sequenceDiagram
    participant U as ユーザー
    participant Hook as KeyboardHook
    participant Form as LauncherForm
    participant FS as FuzzySearcher
    participant Menu as $script:Menus[CurrentMenu]

    U->>Hook: 文字キー押下
    Hook->>Form: Invoke-LauncherInputChar
    Form->>FS: Invoke-FuzzySearcherChar
    FS->>FS: CurrentQuery += 文字
    FS->>FS: Update-FuzzySearcherView
    FS->>Menu: 全アイテムに Get-FuzzyMatchScore
    Menu-->>FS: Score/Bingo 付きリスト
    FS->>FS: Score降順 + 元順序で安定ソート
    FS->>FS: Update-FuzzySearcherResultList（1件目を自動選択）
    FS->>FS: Set-FuzzySearcherLayout / 位置調整
    FS-->>U: 候補リスト再描画
```

- ソートはスコア降順、同点は元のインデックス順で **安定ソート**（PowerShell の `Sort-Object` が非安定なため明示的に実装）。
- `&toggleDebug` で `Score` / `Bingo` 列を表示できる。

---

## 2. コマンド定義ファイル（`commands/*.txt`）

実装: `Core/CommandListParser.ps1`（読込は `Core/CommandManager.ps1` の `Import-CommandList` から）

### 2.1 ファイル読み込みフロー

```mermaid
flowchart TD
    Start(["commands フォルダの *.txt, *.log を\nファイル名順に走査"]) --> ForFile["1ファイルずつ処理\n(開始時は常にルートメニューへ)"]
    ForFile --> ForLine["1行ずつ Trim して判定"]
    ForLine --> Blank{"空行 or # で始まる?"}
    Blank -- Yes --> Skip["読み飛ばし"] --> ForLine
    Blank -- No --> Percent{"% で始まる?"}
    Percent -- Yes --> Group["メニュー切替\n以後の行はこのグループに所属\n(同名を再度開いたら追記)"] --> ForLine
    Percent -- No --> Var{"${ で始まる?"}
    Var -- Yes --> VarDef["変数定義 ${name} = value\n値中の ${..} を先に展開して保存"] --> ForLine
    Var -- No --> Cmd["コマンド行\nCaption, Name, CommandLine"]
    Cmd --> Split["Split-EscapedString(',')\nでエスケープ考慮の3分割"]
    Split --> Enough{"3フィールド\n揃っている?"}
    Enough -- No --> Discard["行を破棄"] --> ForLine
    Enough -- Yes --> Expand["CommandLine を\n Expand-CommandVariable で展開"]
    Expand --> Dup{"同名(Name)が\n同メニューに既存?"}
    Dup -- Yes --> Replace["旧エントリを削除し新しい方を採用\n(後勝ち)"]
    Dup -- No --> Add["メニューに追加"]
    Replace --> ForLine
    Add --> ForLine
    ForLine -->|次ファイルへ| ForFile
```

- 行単位のエラー（未定義変数参照など）は `try/catch` で個別に捕捉し、その行だけスキップして残りは継続読込。
- メニュー名の比較は大文字小文字を区別（`Ordinal`）、コマンド名(`Name`)の重複判定は区別しない（`OrdinalIgnoreCase`）。

### 2.2 データモデル

```mermaid
classDiagram
    class Menus {
        Dictionary~string, List~CommandItem~~
        キー "" = ルートメニュー
    }
    class CommandItem {
        string Caption
        string Name
        string CommandLine
    }
    Menus "1" o-- "0..*" CommandItem : グループごとに保持
```

### 2.3 行フォーマット早見表

| 行の先頭 | 意味 | 例 |
|---|---|---|
| （なし・空行） | 無視 | |
| `#` | コメント | `# ここに説明` |
| `%` | メニュー（グループ）見出し | `%りそな` |
| `${` | 変数定義 | `${OneDrive} = C:\Users\...` |
| それ以外 | コマンド行（`表示名, コマンド名, 実行コマンド`） | `feni, feni, "%FENI_PATH%\bootstrap.bat"` |

### 2.4 サンプル（`default.txt` 抜粋）

```txt
${OneDrive} = C:\Users\dobashi\OneDrive - ...
${filer}    = "${OneDrive}\...\MDIE.exe"

!Add command,   addcommand, &addCommand
りそな,          risona,     &cmdList りそな

%りそな
出納帳確認,      osahou,     ${filer} "${OneDrive}\...\出納帳..."
戻る,            exit,       &cmdList
```

---

## 3. 変数解釈

FuzzyLauncher には性質の異なる **2種類の変数展開** があり、解決タイミングが違う点が最重要ポイント。

| 種類 | 構文 | 解決タイミング | 解決者 | 定義場所 |
|---|---|---|---|---|
| 自作変数 | `${name}` | **読み込み時**（起動時 / コマンド追加後の再読込時） | `CommandListParser.ps1` の `Expand-CommandVariable` | `commands/*.txt` 内 `${name} = value` |
| OS環境変数 | `%ENV_VAR%` | **実行時** | `cmd.exe`（FuzzyLauncher 自身は展開しない） | Windowsの環境変数（レジストリ） |

> `{query}` `{clipboard}` のような「実行のたびに変わる」プレースホルダは **存在しない**。クリップボードは `&Copy` で書き込む用途のみで、コマンド行内で読み出す構文は無い。

### 3.1 二段階解決のシーケンス

```mermaid
sequenceDiagram
    participant File as commands/*.txt
    participant Parser as CommandListParser
    participant Item as CommandItem.CommandLine<br>(メモリ上に確定済み文字列)
    participant LF as LauncherForm
    participant Cmd as cmd.exe
    participant Env as プロセス環境変数<br>(EnvironmentSyncで最新化)

    Note over File,Item: ①読み込み時（起動 or 再読込のたび）
    File->>Parser: ${OneDrive} = C:\Users\...
    Parser->>Parser: 値中の ${..} を先に展開して辞書へ格納
    File->>Parser: filer, filer, ${OneDrive}\tool.exe
    Parser->>Parser: Expand-CommandVariable で ${OneDrive} を置換
    Parser->>Item: 展開済み文字列を CommandItem.CommandLine に確定保存

    Note over Item,Env: ②実行時（コマンド選択のたび）
    LF->>Item: CommandLine を取得（%FENI_PATH%等はまだ文字のまま）
    LF->>Cmd: cmd /c start "" <CommandLine>
    Cmd->>Env: %ENV_VAR% を現在の環境変数で展開
    Cmd-->>LF: 実プロセス起動
```

- `${name}` は **前方参照不可**（定義前に参照すると `未定義の変数: ${name}` で例外→その行だけスキップ）。同ファイル内はもちろん、アルファベット順で後に読まれる別ファイルからも前のファイルの変数を参照できる。
- `%ENV_VAR%` は `&` 始まりの内製コマンドには効かない（`cmd.exe` を経由しないため）。外部プロセス起動行のみ有効。
- `Utils/EnvironmentSync.ps1` が起動時に一度、レジストリから直接環境変数を読み直しプロセスに反映する（ログオンし直さなくても新しい `%VAR%` を使えるようにするための仕組み）。

---

## 4. 独自構文

実装: `Core/BuiltinCommandManager.ps1` + `Core/BuiltinCommands/*.ps1`（`&`構文）、`UI/LauncherForm.ps1`（`;`連結）、`Utils/StringHelper.ps1`（エスケープ）

### 4.1 記号の役割まとめ

| 記号 | 位置 | 役割 |
|---|---|---|
| `&` | `CommandLine` セグメントの先頭 | 内製コマンド呼び出し |
| `;` | `CommandLine` 内 | 複数コマンドを順番に実行（チェーン） |
| `,` | コマンド定義行 | `Caption, Name, CommandLine` のフィールド区切り |
| `\` | `,` `;` の直前 | 区切り文字のエスケープ（`\,` `\;`） |
| `%` | 行頭 | メニュー見出し（コマンド定義ファイル側の構文） |
| `%...%` | `CommandLine` 内 | OS環境変数（`cmd.exe` が展開） |
| `${...}` | どこでも | 自作変数の参照／定義 |

### 4.2 `&` 内製コマンドのディスパッチ（最長一致）

```mermaid
flowchart TD
    Line["CommandLine セグメント"] --> Amp{"'&' で始まる?"}
    Amp -- No --> NotHandled["NotHandled\n→ 外部プロセスとして実行"]
    Amp -- Yes --> Strip["先頭の & を除去"]
    Strip --> Sort["登録済みprefixを\n文字列長の長い順に走査"]
    Sort --> Match{"prefixと前方一致?\n(大文字小文字無視)"}
    Match -- No --> Sort
    Match -- Yes --> Handler["対応するハンドラを実行\n(arg = prefix以降の文字列)"]
    Handler --> Result{"戻り値"}
    Result --> Close["SuccessClose\nランチャーを閉じる"]
    Result --> Keep["SuccessKeepOpen\nランチャーを開いたまま\n(例: &cmdList, &Sleep)"]
```

長い prefix を先に試すのは、`activeTitle ` と `waitActiveTitle ` のような包含関係のある prefix を正しく区別するため。

### 4.3 主な内製コマンド一覧

| コマンド | 効果 | 戻り値 |
|---|---|---|
| `&cmdList [menu]` | メニュー切替（引数なしはルートへ）＋入力欄リセット | KeepOpen |
| `&Copy <text>` | クリップボードへコピー | Close |
| `&url <url>` | 既定ブラウザで開く | Close |
| `&activeTitle/&activeExe/&activeClass <値>` | 該当ウィンドウをアクティブ化 | Close |
| `&waitActiveTitle/&waitActiveExe/&waitActiveClass <値>` | 該当ウィンドウが前面に来るまでポーリング待機（タイムアウトで例外） | KeepOpen(通常) |
| `&SendKeys <keys>` | ランチャーを閉じてから `SendKeys.SendWait` | Close |
| `&Sleep <ms>` | UIポンピングしながら待機 | KeepOpen |
| `&addCommand` | コマンド追加ダイアログを開く | Close |
| `&editThisCommand` | `default.txt` をメモ帳で開く | Close |
| `&reloadThisApp` / `&exitThisApp` | 再起動 / 終了 | Close |
| `&toggleDebug` | 検索エンジンの Score/Bingo 列表示切替 | Close |

### 4.4 `;` チェーン実行のシーケンス

```mermaid
sequenceDiagram
    participant U as ユーザー(Enter/Tab)
    participant LF as LauncherForm
    participant BM as BuiltinCommandManager
    participant OS as cmd.exe / Process.Start

    U->>LF: Invoke-LauncherItemSelected
    LF->>LF: Close-Launcher (IsActive=false)
    LF->>LF: CommandLineをセミコロンで分割 (Split-EscapedString)
    loop 各サブコマンドを順に
        LF->>BM: Invoke-BuiltinCommand(sub)
        alt & で始まる
            BM-->>LF: SuccessClose / SuccessKeepOpen
        else 外部コマンド
            LF->>OS: cmd /c start "" サブコマンド (%ENV%はここで展開)
            OS-->>LF: NotHandled扱い→SuccessClose相当
        end
        Note over LF: 例外発生時は残りのサブコマンドを中断しエラーダイアログ表示
    end
    LF->>LF: 最後のサブコマンドの結果でポップアップを閉じたままにするか再表示するか決定
```

例: `notepad.exe; &waitActiveTitle メモ帳; &Dialog メモ帳が起動しました！`
→ メモ帳起動 → ウィンドウ出現を待機 → ダイアログ表示、の3段階を1行で記述できる。

### 4.5 エスケープ規則

`Utils/StringHelper.ps1` の `Split-EscapedString` が `,`（コマンド定義の3分割）と `;`（実行時チェーン分割）の両方で共用される。

- `\,` → 区切りとして扱わずリテラルの `,`
- `\;` → 区切りとして扱わずリテラルの `;`
- `\\,` → 直前1文字だけ見るため分割されない（`\\,`→`\,`のリテラルとして残る、連続エスケープの直前1文字のみを見る仕様）

```mermaid
flowchart LR
    Raw["生の行文字列"] --> Regex["直前が '\\' でない\n区切り文字で分割\n(否定後読み)"]
    Regex --> Unescape["各フィールド内の\n\\, \\; \\\\ を実文字へ戻す"]
    Unescape --> Fields["Caption / Name / CommandLine\nまたは サブコマンド配列"]
```

---

## 5. エンドツーエンド全体シーケンス

```mermaid
sequenceDiagram
    participant U as ユーザー
    participant Hook as KeyboardHook(グローバルフック)
    participant Form as LauncherForm
    participant FS as FuzzySearcher
    participant Parser as CommandListParser
    participant BM as BuiltinCommandManager
    participant OS as OS/外部プロセス

    Note over Hook,Parser: 起動時に一度だけ
    Form->>Parser: Import-CommandList
    Parser-->>Form: $script:Menus 構築完了(変数展開済み)

    U->>Hook: トリガーキー(かな/変換等)
    Hook->>Form: Invoke-LauncherToggle
    Form->>FS: Show-FuzzySearcher(ルートメニューの候補)
    Hook->>Hook: IsActive = true(以後キー全捕捉)

    loop 文字入力のたび
        U->>Hook: 文字キー
        Hook->>FS: Invoke-FuzzySearcherChar
        FS->>FS: Get-FuzzyMatchScore で再スコアリング・再ソート
        FS-->>U: 候補リスト更新
    end

    U->>FS: Enter/Tab で確定
    FS->>Form: OnSelect(選択されたCommandItem)
    Form->>Form: Close-Launcher
    Form->>Form: CommandLineをセミコロンで分割
    loop サブコマンドごと
        Form->>BM: Invoke-BuiltinCommand
        alt 内製コマンド(&)
            BM->>BM: ハンドラ実行(cmdList/Copy/url等)
        else 外部コマンド
            Form->>OS: cmd /c start "" ...(%ENV%展開)
        end
    end
    alt 最後がSuccessKeepOpen(&cmdListで潜った等)
        Form->>FS: 再表示(サブメニューの候補で)
    else SuccessClose
        Form->>Hook: IsActive = false
    end
```

---

## 参考ソース

- `UI/FuzzySearcher.psm1`（検索エンジン本体）
- `Core/CommandListParser.ps1`（コマンド定義ファイルのパース／変数展開）
- `Core/CommandManager.ps1`（メニュー状態管理）
- `Core/BuiltinCommandManager.ps1` + `Core/BuiltinCommands/*.ps1`（`&`独自構文）
- `UI/LauncherForm.ps1`（`;`チェーン実行、外部プロセス起動）
- `Utils/StringHelper.ps1`（エスケープ付き分割）
- `Utils/EnvironmentSync.ps1`（`%ENV%`のための環境変数同期）
